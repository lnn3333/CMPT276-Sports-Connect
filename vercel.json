{
  "version": 2,
  "builds": [
    { "src": "app.py", "use": "@vercel/python" }
  ],
  "config": [
   { "command": "python3 setup.py install"}
],
  "routes": [
    { "src": "/(.*)", "dest": "/app.py" }
  ]
}

