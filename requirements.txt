
Flask==2.0.1
pytz==2023.10  # Specify a version that works for your project
numpy==1.21.3  # Specify a version that works for your project
nba-api-client==2.0.0  # Assuming nba_api is a custom module; specify the correct package if it's an external library
requests==2.26.0  # Specify a version that works for your project
http.client
json
datetime

