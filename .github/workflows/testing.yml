name: Run Tests on Main Branch Merge

on:
  push:
    branches:
      - main

jobs:
  test-on-main-merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: Testing

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9.13

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run tests
        run: |
          cd $GITHUB_WORKSPACE/tests
          python3 tests-awards.py
          python3 tests-news.py
          python3 tests-playerProf.py
          python3 tests-schedule.py
          python3 tests-teamStats.py
